// Tilesets will have to be added by hand here. Ideally, we would have a registry where developers can
// add their own tilesets via code and setting their index (or id)

import { Tiles_Walls } from "./walls/tileset";

export const VoxelTileSets = [
    null,
    Tiles_Walls
]